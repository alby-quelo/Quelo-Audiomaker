# partition.conf Quelo — metadati EFI/swap.
# Layout partizioni: NON usato in exec. Fonte unica:
#   /etc/quelo-install-choices.yaml  →  modulo quelo-partition-disk
# (Il wizard layout può riscrivere questo file senza partitionLayout.)
---
efi:
    mountPoint:         "/boot/efi"
    recommendedSize:    512MiB
    minimumSize:        32MiB
    label:              "EFI"

userSwapChoices:
    - none
    - small
    - suspend
    - file

luksGeneration: luks1
drawNestedPartitions: false
alwaysShowPartitionLabels: true
allowManualPartitioning: false
initialPartitioningChoice: erase
defaultFileSystemType: "ext4"
essentialMounts: [ "live-*", "control", "ventoy" ]
